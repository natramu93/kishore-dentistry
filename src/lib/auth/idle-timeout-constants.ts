/** Shared idle-session policy used by the server boundary and browser watcher. */
export const IDLE_TIMEOUT_SECONDS = 10 * 60;
export const IDLE_COOKIE_NAME = "crm-idle-session";
export const IDLE_COOKIE_MAX_AGE_SECONDS = 60 * 60 * 24 * 30;
